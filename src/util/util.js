import config from './config'
import mock from './mock'
import axios from 'axios'

var utils = {
    getUserSession(cb) {

    },
    sendRequest() {

    },
    requestFun() {
        
    }
}

export default utils